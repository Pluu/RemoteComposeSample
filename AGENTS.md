# Agent Workflow Guidelines

이 문서는 이 프로젝트에서 작업하는 AI 에이전트를 위한 작업, 검증 및 커밋 단계의 워크플로우를 정의합니다.

## 1. 작업 (Work) 단계
- **요구사항 분석**: 사용자의 요청을 명확히 이해하고, 현재 코드베이스의 구조와 아키텍처를 파악합니다.
- **코드 수정 및 생성**:
    - 프로젝트의 기존 스타일과 패턴을 준수합니다.
    - **Single Import**: 와일드카드(`*`) 대신 항상 명시적인 단일 임포트를 사용합니다.
    - 적절한 도구(`write_file`, `replace_file_content`, `multi_replace_file_content`)를 사용하여 정확하게 수정합니다.
- **문서화**: 새로운 기능이나 중요한 변경 사항이 있는 경우 관련 문서(`README.md`, `.artifact.md`)를 업데이트합니다.

## 2. 검증 (Verification) 단계
- **빌드 및 동기화**: 빌드 스크립트(`build.gradle.kts`, `libs.versions.toml`)가 변경된 경우 반드시 `gradle_sync`를 실행합니다.
- **배포 및 실행**: `deploy` 도구를 사용하여 앱을 에뮬레이터나 기기에 배포합니다.
- **UI 검증**:
    - `ui_state`와 `take_screenshot`을 사용하여 의도한 UI가 올바르게 표시되는지 확인합니다.
    - 서버-앱 간의 통신이 필요한 경우 `adb reverse` 등 네트워크 구성을 확인합니다.
- **로그 분석**: `read_logcat`을 통해 런타임 에러, 경고 또는 중요한 디버그 메시지를 모니터링합니다.

## 3. 커밋 (Commit) 단계
- **논리적 단위 분리**: 서로 다른 관심사의 작업은 별도의 커밋으로 나눕니다 (예: 종속성 추가, 서버 구현, 앱 구현을 각각 분리).
- **커밋 메시지 표준**: 작업의 성격을 명확히 알 수 있는 접두사를 사용합니다.
    - `Feat`: 새로운 기능 추가
    - `Fix`: 버그 수정
    - `Refactor`: 코드 리팩토링
    - `Config`: 설정 변경 (Gradle, Network 등)
    - `Docs`: 문서 작업
- **커밋 전 확인**: `git status`를 사용하여 누락된 파일이나 불필요한 파일이 포함되지 않았는지 확인합니다.
